az login

# Get cluster credentials
az aks get-credentials --resource-group fhwn-19-kubernetes-1 --name 105553cluster

# Store mysql root password as secret
kubectl create secret generic mysql-pass --from-literal=password=topSecret

# Create the resources specified in the *.yml files
kubectl apply -f ./mysql-deployment.yml
kubectl apply -f ./wordpress-deployment.yml